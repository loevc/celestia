server:
  port: 6104
management:
  server:
    port: 16104
spring:
  application:
    name: @artifactId@
  #配置中心
  cloud:
    nacos:
      discovery:
        server-addr: http://10.20.0.3:30048
        namespace: d6c8dc3f-1c32-4222-9a63-9b36fd51b249
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}
        file-extension: yml
        shared-configs:
          - data-id: application-${spring.profiles.active}.${spring.cloud.nacos.config.file-extension}
            refresh: true
        namespace: ${spring.cloud.nacos.discovery.namespace}
  profiles:
    active: celestia

  rabbitmq:
    addresses: 10.20.0.3
    port: 5672
    username: dev
    password: hexiaoqiang
    virtual-host: celestia
    publisher-confirm-type: correlated
    publisher-returns: true